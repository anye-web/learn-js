const num1 = 100;
const num2 = 50;
let val;

// Basic maths
val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;

// Math object
val = Math.PI;
val = Math.E;
// Round up
val = Math.round(2.4);
// Round up
val = Math.ceil(2.4);
// Round Down
val = Math.floor(2.4);
// Square root
val = Math.sqrt(64);
// Get absolute Number
val = Math.abs(-3);
// get power
val = Math.pow(8, 2);
// Get the min and max
val = Math.min(2, 34, 56, -9, 56, 10);
val = Math.max(2, 34, 56, -9, 56, 10);
// Get a Random Number
val = Math.random();

val = Math.floor(Math.random() * 20 + 1);


console.log(val);